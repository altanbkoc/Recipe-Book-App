<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.YemekKitabı" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/kahve</item>
        <item name="colorPrimaryVariant">@color/acikkahve</item>
        <item name="colorSecondary">@color/kahve</item>

        <!-- Varsayılan metin renkleri -->
        <item name="android:textColorPrimary">#2C2A2A</item>
        <item name="android:statusBarColor">@color/kahve</item>
        <item name="android:navigationBarColor">@color/black</item>
    </style>
</resources>